<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>Blog</title>
        {% block stylesheets %}   
            {% include "source.html.twig" %}
        {% endblock %}
    </head>
    <body>
        {% block nav %}
            {% include 'navbar.html.twig' %}
        {% endblock %}
        <!-- Page Header-->
        <header class="masthead" style="background-image: url('assets/img/desktop.jpg')">
            <div class="container position-relative px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-md-10 col-lg-8 col-xl-7">
                        <div class="post-heading">
                            <div class="row">
                                <div class="col-xl-2 col-md-3 col-sm-4">
                                    <form action="model/deletePost.php" method="post">
                                        <input type="hidden" value="{{ idPost }}" name="iddel"/>
                                        <input type="submit" class="btn btn-danger" value="Supprimer"/>
                                    </form>
                                </div>
                                <div class="col-xl-2 col-md-3 col-sm-4">
                                    <a href="index.php?action=postUpdate&amp;id={{ idPost }}" class="btn btn-success">Modifier</a>
                                </div>
                            </div>
                            <h1>{{ title }}</h1>
                            <h2 class="subheading">{{ descriptionPost }}</h2>
                            <span class="meta">
                                Posté par {{ authorPost }} le {{ creationDate }}
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </header>
        <!-- Post Content-->
        <article class="mb-4">
            <div class="container px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-md-10 col-lg-8 col-xl-7">
                        {{ contentPost }}
                    </div>
                </div>
            </div>
        </article>
        <div class="container">
            <div class="be-comment-block">
                <h1 class="comments-title">Commentaires</h1>
                {% for rowComment in dataComment %}
                    <div class="be-comment">
                        <div class="be-img-comment">	
                            <img src="https://bootdey.com/img/Content/avatar/avatar1.png" alt="" class="be-ava-comment">
                        </div>
                        <div class="be-comment-content">
                                <span class="be-comment-name">
                                    {{ rowComment.pseudo }}
                                </span>
                                <span class="be-comment-time">
                                    {{ rowComment.creationDate }}
                                </span>

                            <p class="be-comment-text">
                                {{ rowComment.content }}
                            </p>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
        <!-- Footer-->
        {% block footer %}   
            {% include "footer.html.twig" %}
        {% endblock %}
        <!-- Bootstrap core JS-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Core theme JS-->
        <script src="js/scripts.js"></script>
    </body>
</html>
